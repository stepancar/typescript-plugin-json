{
  "name": "typescript-plugin-json",
  "version": "0.0.1-beta.0",
  "main": "lib/index.js",
  "homepage": "https://github.com/stepancar/typescript-plugin-json",
  "files": [
    "lib"
  ],
  "repository": "https://github.com/stepancar/typescript-plugin-json",
  "author": "stepancar <stepancar@hotmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf ./lib && tsc",
    "lint": "eslint './src/**/*.{js,ts}' --max-warnings 0 && yarn prettier './**/*.{json,md,js,ts,yml}' -c",
    "prepublishOnly": "yarn build",
    "test": "jest ./src"
  },
  "keywords": [
    "json",
    "modules",
    "plugin",
    "typescript"
  ],
  "dependencies": {
    "typescript": "^3.8.3"
  },
  "peerDependencies": {
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "@types/node": "^14.14.7"
  }
}
